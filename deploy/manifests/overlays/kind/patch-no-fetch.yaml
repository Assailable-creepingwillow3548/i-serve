# Delete the download Job on kind: the stub serves no model, and what the base
# would otherwise pull onto a laptop is priced in docs/running-on-kind.md. The
# PVC itself is kept: what is under test here is volume binding and scheduling,
# and that is real on kind.
$patch: delete
apiVersion: batch/v1
kind: Job
metadata:
  name: fetch-weights
