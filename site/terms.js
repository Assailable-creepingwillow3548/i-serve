/* terms.js -- the page's vocabulary, explained in plain words on hover.
 *
 * Every term here is registered in docs/GLOSSARY.md (bench/tests/test_site_terms.py
 * holds that), and `glossary` names the entry it maps to. The `plain` text is
 * not a copy of the glossary's definition and is not meant to be one: the
 * glossary is written for an engineer looking a term up, this is written for
 * a reader meeting it for the first time. Two jobs, two texts, one registry.
 * `match` lists the spellings ui.js highlights; longest wins where they nest.
 */
window.TERMS = [
  { glossary: "Token", match: ["tokens", "token"],
    plain: "The unit a model reads and writes: a word piece, about three quarters of an English word on average. Prompts, answers, speeds and prices here are all counted in tokens." },
  { glossary: "SLO", match: ["SLO", "SLOs"],
    plain: "Service Level Objective: the promise you make about speed, e.g. \"99 % of tokens arrive within 50 ms\". You choose it from what your users need and what you will pay; the page prices it." },
  { glossary: "TTFT", match: ["TTFT", "first token", "first-token"],
    plain: "Time To First Token: how long a user waits before the first word appears. Mostly the time to read the whole prompt once (prefill)." },
  { glossary: "TPOT", match: ["TPOT", "ms per token", "per token"],
    plain: "Time Per Output Token: the gap between two consecutive words as the answer streams. 50 ms per token is 20 words a second, about three times reading speed." },
  { glossary: "p50 / p95 / p99", match: ["p99", "p50"],
    plain: "A percentile. \"TTFT p99 = 300 ms\" means 99 of 100 requests got their first token within 300 ms. Tails matter more than averages: users notice the slow one." },
  { glossary: "max_num_seqs", match: ["seats", "seat", "max_num_seqs"],
    plain: "A seat is one request being answered right now. The card answers all seats together, one token each per step; max_num_seqs is the cap on how many." },
  { glossary: "Decode", match: ["decode step", "decode"],
    plain: "Producing the answer, one token at a time. Each step reads the whole model plus every seat's KV cache from memory, so more seats means a slower step." },
  { glossary: "Prefill", match: ["prefill", "prefills"],
    plain: "Reading the prompt before the first answer token: all prompt tokens are computed at once. Long prompts mean a long wait for the first token." },
  { glossary: "KV cache", match: ["KV cache", "KV pool", "KV", "pool"],
    plain: "Key-Value cache: the memory each seat keeps of the text it has seen so far, so it is not re-read every step. It grows with the context and is what fills the card up." },
  { glossary: "Prefix caching", match: ["prefix cache", "prefix caching", "cached", "cache"],
    plain: "Reusing the KV of a prompt beginning many requests share (a system prompt, examples), so that part is not computed again. The hit rate h is the share of each prompt it already holds." },
  { glossary: "Floor", match: ["floor", "floors"],
    plain: "The best the hardware could possibly do — the time to move the bytes or do the arithmetic, and nothing else. A real system is always slower; the gap is queueing and overhead." },
  { glossary: "Queue depth", match: ["queue", "waiting"],
    plain: "Requests admitted but not yet given a seat. A growing queue means the card is full; the fix is more replicas, not a setting." },
  { glossary: "Deployment", match: ["replicas", "replica"],
    plain: "One more copy of the whole server on one more card. Replicas add seats but each reads the model weights again, so they are not free throughput." },
  { glossary: "Prefill interference", match: ["prefill interference", "interference"],
    plain: "When a new request arrives, its prefill is squeezed into other seats' decode steps and slows them. It is why a service can promise fewer seats than the hardware alone would allow." },
  { glossary: "Memory-bound", match: ["memory bus"],
    plain: "The path between the card's memory and its compute units. Decode is limited by how many bytes per second it can move, not by arithmetic — hence \"the limit is time\"." },
  { glossary: "Parameters (P)", match: ["weights"],
    plain: "The model's learned numbers, about 16 GB for Qwen3-8B in BF16. Read in full at every decode step, whatever the batch — a fixed cost every token shares." },
  { glossary: "Context length", match: ["context length", "context"],
    plain: "Prompt plus answer so far: the tokens a seat holds in its KV cache. A seat reserves room for prompt + output tokens." },
  { glossary: "FP8 KV cache", match: ["FP8", "BF16", "KV dtype", "dtype"],
    plain: "How many bytes each stored number takes. BF16 uses two, FP8 one — so FP8 halves the KV cache, doubling both the room limit and the time limit, but leaving their ratio alone." },
  { glossary: "gpu_memory_utilization", match: ["gpu_memory_utilization"],
    plain: "The share of the card's memory the engine may use, 0.90 by default. Weights come out of it first; what is left is the KV pool. The engine's own startup log says the real figure." },
  { glossary: "Coefficient provenance", match: ["prior"],
    plain: "Where a card's efficiency numbers come from: measured on a rented card in a run, or a prior — a spec-sheet assumption no run has faced yet. A prior can be badly wrong." },
  { glossary: "`kind`", match: ["kind"],
    plain: "Kubernetes in Docker: the whole stack runs on a laptop with no GPU, against a stub that behaves like the engine but serves no model. It shows what the system does, never what the numbers are." },
  { glossary: "eff_mem", match: ["eff_mem"],
    plain: "The share of the card's advertised memory bandwidth a decode step actually achieves — 0.83 measured on the L40S. The spec sheet is never reached." },
  { glossary: "MFU", match: ["mfu"],
    plain: "Model FLOPs Utilisation: the share of the card's advertised arithmetic speed that prefill actually achieves — 0.44 measured on the L40S." },
  { glossary: "Chunked prefill", match: ["chunked prefill"],
    plain: "Splitting a long prompt's prefill into pieces interleaved with other seats' decode steps: everyone's tokens arrive more evenly, the first token a little later." },
  { glossary: "Goodput", match: ["goodput"],
    plain: "Throughput counting only the requests that met the promise. Raw throughput can keep rising while every user is already breaching." },
  { glossary: "Preemption", match: ["preemptions", "preemption"],
    plain: "The engine taking a seat's KV cache back because the pool is full; the request waits and its prompt is read again later. A climbing count means the card is out of room." },
  { glossary: "Runsheet", match: ["run 1", "run 2", "run 3", "runs 1–3", "a run"],
    plain: "A rented-card session with its predictions written down beforehand. This repository has three, all on an L40S; every measured point on this page comes from one of them." },
  { glossary: "Dashboard as code", match: ["dashboard", "row 2", "row 3"],
    plain: "The Grafana board the stack ships with, read top to bottom: row 1 is whether the promise holds, row 2 what users get, row 3 where the gap is — queue, seats, KV, preemptions." },
  { glossary: "Workload class", match: ["batch class", "workload class", "batch pipeline"],
    plain: "Which obligation you are under. Interactive: latency is the product. Batch: the promise is tokens delivered before a deadline, and the per-token threshold is only a guard against pathological stalls." },
  { glossary: "Stall guard", match: ["stall guard"],
    plain: "The batch class's per-token threshold, named for its job: it catches a pathological stall, it is not a promise. Tightening it caps the batch size and raises the price per million tokens to improve a number nobody watches." },
  { glossary: "Throughput", match: ["throughput", "tokens per second", "output tokens per second"],
    plain: "Total tokens per second the whole server emits across every seat at once. Batching multiplies it while each single user's tokens come slower — which is why a chat user and a batch pipeline read opposite numbers." },
  { glossary: "Continuous batching", match: ["batch"],
    plain: "The engine answering many requests together, one token each per step, adding and removing seats as they come and go. More seats: more tokens per second in total, each one slower." },
];
