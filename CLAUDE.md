# CLAUDE.md

Orientation for an agent working in this repository. **This file holds no facts of
its own** — it says where each fact lives and which file wins on a conflict.
Everything here is a pointer, deliberately, so there is nothing to drift out of
date.

## What this is

A slice of an LLM inference operator's platform — see `README.md` for the legend,
the model choice and the layout. Scale is deliberately small: the point is that
the categories of reasoning are the ones a real operator uses, and that every
number is traceable to the run or the derivation that produced it.

## Source of truth

| File | Authority over |
|---|---|
| `README.md` | project legend, model choice, repository layout, how to run it |
| `docs/audience.md` | who the repository is for, what is real on `kind` and what is fixture, and the measured / derived / assumed split |
| `docs/SLO.md` | targets, floors, concurrency ceiling, cost formula, empirical coefficients, and how each was derived |
| `docs/GLOSSARY.md` | vocabulary and notation, including architectures that break the standard formulas |
| `docs/accelerator-landscape.md` | the accelerator market: vendors, their attack surface on the decode equation, and the freshness of each claim |
| `docs/architecture.md` | the request path from ingress to GPU |
| `docs/runbook.md` | canary rollout, OOM handling, morning triage |
| `docs/benchmarks/` | measured results; predicted-vs-measured tables |
| `docs/benchmarks/runsheets/` | per-run checklists: the commands and the predicted numbers a run must face, written before GPU money is spent |
| `docs/instrument-vllm-bench-sweep.md` | what `vllm bench sweep` does at a pinned tag, and where it belongs in a run |
| `docs/symptom-map.md` | the operator's decision tree: symptom → mechanism → knob, one line per node with its evidence and its home; `docs/symptom-map.json` is the checked subset the site's advisor evaluates |
| `docs/adding-a-run.md`, `CONTRIBUTING.md` | what a contribution is, and the procedure a new run follows |
| `site/README.md` | how the page is built, and the three guards that hold its JavaScript equal to `bench/` |

Rules of the map: a fact belongs to exactly one file, and other files link to it
instead of restating it. If two files disagree, the one with authority above wins
and the other is wrong.

## Layout

The path-by-path table is in `README.md`, with a state column. Directories map
one-to-one onto the components the capstone is built from; keep that mapping —
the tree is meant to read as a map of the work.

Not every directory in the working tree is part of the repository. Anything the
root `.git/info/exclude` holds out belongs to another repository on another
remote, and a tracked file must never reference it — no path, no filename. A
commit that does is a defect whatever else it gets right.

## Conventions

- **Language.** Repository content is English without exception.
- **Units, hardware figures, model figures, stated assumptions.** SI base units;
  dense FLOPS, never the sparsity row; `config.json` for architecture; and a
  derived floor is never reported as a measurement.
- **New vocabulary.** Every term, abbreviation, metric, formula symbol or named
  config knob introduced by a change is written into `docs/GLOSSARY.md` in the
  same change — definition only, with any argument left in `docs/SLO.md`.
- **Code.** Small steps, every line explained; no large unprompted blocks.
- **Cost.** Anything requiring GPU credits is flagged with its cost in
  credit-hours before running. Everything that can run on `kind` or CPU does.
- **Measurement outranks derivation.** Every number in `docs/SLO.md` and
  `bench/roofline.py` is a prediction until measured. vLLM logs the actual KV
  cache size and block count at startup — that log outranks any derivation in
  this repo (`docs/SLO.md` §9).
- **Generated files are generated.** `docs/benchmarks/predicted-vs-measured.svg`
  and `site/data/` are produced by `bench/`, regenerated by the pre-commit hook
  and asserted by CI; edit the source, never the output.
